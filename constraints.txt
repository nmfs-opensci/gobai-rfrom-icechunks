# Exact versions for requirements.lock -- see setup.md, "Pinned install".
#
# Recorded versions the published RFROM v2.3 Icechunk store was built and
# validated with (claude/notes/rfromv-icechunk.md, §11 "Versions"), on Python 3.12.12.
icechunk==2.2.0
virtualizarr==2.2.1
xarray==2025.12.0
zarr==3.1.5
obstore==0.8.2
gcsfs==2025.12.0
h5py==3.13.0
numpy==2.3.5
# Not recorded there; pinned to the validated lower bounds in requirements.txt.
dask==2025.11.0
h5netcdf==1.7.3
pandas==2.3.3
